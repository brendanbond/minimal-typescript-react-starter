import React from "react";
import styled from "styled-components";

const AppContainer = styled.div`
  font-size: 24px;
  text-align: center;
`;

const App = () => (
  <AppContainer>Welcome to your React typescript starter.</AppContainer>
);

export default App;
